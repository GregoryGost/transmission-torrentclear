# Project Overview

- Backend project using TypeScript only, no web elements.
- Do not import new libraries without permission.
- Use ES imports, not require().

## Project Description

This application is required for automatically cleaning downloaded media torrent files using the external application `transmission-daemon` and its commands.

The project uses unit tests.
